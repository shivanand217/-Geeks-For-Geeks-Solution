// AUTHOR: SHIV ANAND
// LANGUAGE: CPP
// TIME COMPLEXITY : O(max(n1,n2)) // n1, n2 is the length of both the strings
// SPACE COMPLEXITY : O(max(n1,n2))

#include<bits/stdc++.h>
#include<cstdio>
#include<cstdlib>
#define read(val) scanf("%d",&val);
#define print(val) printf("%d ",val);
#define scan(val) scanf("%lld",&val);
#define show(val) printf("%lld ",val);
#define rr(val) for(int i=0; i<val; i++)
#define LL long long
#define mod 1000000007
using namespace std;

string calculate_distance(string s1,string s2)
{
    string result = "";

    if(s1.length() < s2.length())
    {

    }
    else
    {

    }
}

int main(){

string s1,s2;
cin>>s1>>s2;

string p = calculate_distance(s1,s2);
// obtained distance
cout<<p<<endl;

return 0;
}
